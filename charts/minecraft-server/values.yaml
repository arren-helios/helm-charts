# Default values for minecraft.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

image:
  repository: docker.helios.a/minecraft-server
  pullPolicy: IfNotPresent
  tag: ""

service:
  type: ClusterIP
  port: 25565
  loadBalancerIP: ""

persistence:
  enabled: false
  volumeName: ""
  accessModes: [ReadWriteOnce]
  size: 10Gi
  storageClass: ""

resources:
  # limits:
  #   cpu: 2
  #   memory: 4Gi
  # requests:
  #   cpu: 1
  #   memory: 2Gi
